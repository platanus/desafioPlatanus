<h1>Seleccione el evento</h1>

<table>
	<thead>
		<tr>
			<th>Evento</th>
		</tr>
	</thead>
	<tbody>
    <% @events.each do |event| %>
    <tr>
			<td><%= link_to event.name, print_details_path(event.id) %></td>
		</tr>
    <% end  %>
	</tbody>
</table>
